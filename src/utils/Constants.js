const API_URL = "https://api.cyrenecar.com";

export const ENDPOINTS = {
  VEHICLES: `${API_URL}/vehicles`,
  BOOKINGS: `${API_URL}/bookings`,
  REVIEWS: `${API_URL}/reviews`,
};

export const COLORS = {
  PRIMARY: "#1a73e8",
  SECONDARY: "#ffffff",
};

export default { API_URL, ENDPOINTS, COLORS };
